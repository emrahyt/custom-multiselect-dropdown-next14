import React from "react";
import { StyledContainer, StyledSpinner } from "./spinner.styles";

const Spinner: React.FC = () => (
  <StyledContainer>
    <StyledSpinner />
  </StyledContainer>
);

export default Spinner;
